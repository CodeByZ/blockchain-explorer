import { ContainsFilterPipe } from './contains-filter.pipe';

describe('ContainsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContainsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
